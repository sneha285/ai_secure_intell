services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
      - CORS_ALLOW_ORIGINS=*
      # - OPENAI_API_KEY=...

